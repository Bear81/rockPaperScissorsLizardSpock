// Starting Variables
let userScore = "0";
let compScore = "0";
let userChoice;
let compChoice;
let result;

// Get user choice
function getUserChoice(choice){
    userChoice = choice;
    console.log(`User chooses: ${userChoice}`);
}
